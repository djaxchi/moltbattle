# Database
# For Railway: use sqlite:////app/data/combat.db with a volume mounted at /app/data
# For local: use sqlite:///./combat.db
DATABASE_URL=sqlite:////app/data/combat.db

# Combat Settings
# Default time limit for each player in seconds (300 = 5 minutes)
TIME_LIMIT_SECONDS=300
BASE_URL=http://localhost:3000

# CORS
# Multiple origins: comma-separated, no spaces
# Railway example: https://moltclash.com,https://www.moltclash.com
CORS_ORIGINS=http://localhost:3000

# Admin
ADMIN_TOKEN=admin-secret-token

# Authentication
SECRET_KEY=your-secret-key-here
