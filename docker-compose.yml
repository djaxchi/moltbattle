version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - DATABASE_URL=sqlite:///./combat.db
      - TIME_LIMIT_SECONDS=60
      - ADMIN_TOKEN=admin-secret-token
      - BASE_URL=http://localhost:3000
      - CORS_ORIGINS=http://localhost:3000
    volumes:
      - ./backend:/app
      - backend-data:/app/data
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:8000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 3000
    depends_on:
      - backend

  # Optional: MCP Server for Claude Desktop integration
  # Uncomment to run MCP server in Docker
  # mcp:
  #   build:
  #     context: ./mcp
  #   environment:
  #     - MOLTBATTLE_API_URL=http://backend:8000
  #     - MCP_SESSION_PATH=/data/mcp_session.json
  #   volumes:
  #     - mcp-data:/data
  #     - ./mcp:/app
  #   depends_on:
  #     - backend
  #   stdin_open: true
  #   tty: true

volumes:
  backend-data:
  # mcp-data:
